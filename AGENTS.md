# Spark X 自动化测试平台仓库约束

## 仓库边界

- 本仓库是独立的通用自动化测试平台，不属于任何被测系统代码库。
- 默认分支为 `main`，远端固定为 `git@github.com:zhangpang/spark-x-test-platform.git`。
- 星火 Agent 是首个系统适配器；知识库、Skill、MCP、自动任务和最近会话等专有概念只能出现在对应适配器与用例资产中，不得写入平台核心。
- 修改、暂存、提交或推送前必须核对仓库根目录、`origin` 和当前分支；不符合上述目标时停止操作。

## 产品边界

- 平台管理自动化业务回归、API、浏览器、集成和端到端用例。
- 平台的职责是发现、记录、分类、复现和报告问题，不负责也不得直接触发被测业务代码的修改、提交、部署或修复流程。
- 自动化用例失败只是问题证据，不构成修改星火智能体、Runtime、ContiNew 或其他被测系统代码的授权。平台必须先区分产品缺陷、测试用例缺陷、夹具缺陷和环境故障。
- 只有脱离自动化平台、通过产品界面或公开 API 独立复现并确认违反产品需求的问题，才能转成单独的产品缺陷任务；产品修复必须在对应业务仓库、独立提交和独立发布中完成。
- 不得要求被测系统增加仅供自动化使用的登录旁路、专用 Header、专用 Token、明文密码通道、测试专用接口或隐式状态修改。测试必须使用产品已有公开能力和测试环境配置。
- 产品部署流程可以在任何时段自动触发已经稳定配置的回归套件，作为发布后的质量门禁；该回归仍只负责发现、分类和报告问题，不直接触发业务代码修改。
- 用户“下班后手动触发”专指自动化测试平台自身的优化、验证、修改、补充案例和专项回归工作。用户白天工作期间不扩展测试平台功能，优先推进产品功能开发、业务缺陷修复和手工/API 验证，但不禁止部署流程自动执行既有回归。
- 被测项目的单元测试保留在各自仓库；平台可以触发并收集结果，但不复制其测试源码。
- 第一版内部匿名使用，不实现用户登录和角色权限；这不取消网络白名单、操作审计、密钥保护和危险动作限制。
- 第一版 Worker 仅部署于受控内网，不启用 Worker 身份认证；协议必须预留认证字段，跨网络部署前必须启用。
- 禁止从页面上传或执行任意 JavaScript、Python、Shell 或 SSH 命令。可信扩展必须通过仓库代码、审查、构建和不可变镜像发布。

## 开发、验证与部署位置

- 用户本机只用于源码编辑、代码审阅和 Git 操作，不得在本机启动本平台或执行测试验证。
- `npm test`、`npm run check`、应用构建、Docker Compose、数据库迁移、健康检查、浏览器/API 联调、回归测试和故障演练统一在测试环境 `192.168.110.136` 执行。
- GitHub Actions 如需执行测试或构建，只能将任务调度到测试环境上的受控自托管 Runner；GitHub 托管 Runner 和用户本机的结果不得作为验收证据。
- 本平台开发完成后的默认长期部署目标也是测试环境 `192.168.110.136`，除非用户当次明确指定其他部署目标。
- 任何测试、验证或部署操作开始前，都必须先确认目标主机是 `192.168.110.136`；不得在用户本机用等价 Compose 栈代替测试环境证据。

## 数据与安全

- 用例不得保存账号密码、Token、数据库密码或其他密钥，只能引用密钥变量。
- 日志、错误、Trace 和导出数据必须经过统一脱敏。
- HTTP、Webhook 和浏览器动作只能访问已登记系统环境允许的协议、主机与端口；重定向后重新校验。
- 数据库连接默认只读，只允许单条查询，并限制超时和最大返回行数。
- 危险动作只能操作本次运行创建的资源或显式登记的测试资源。
- 每次运行生成唯一 `run_id`，所有可变测试数据必须可追踪、可清理。

## 工程与质量

- 所有前端页面新增或修改必须先使用已安装的 `ui-styling` 前端技能进行设计与实现，统一遵循设计令牌、清晰信息层级、可访问交互和移动端优先响应式原则；不得脱离现有视觉系统临时堆叠样式。
- 前端视觉改动必须在测试环境完成桌面端与移动端浏览器验收，并同时回归原有业务流程，不能只以构建通过作为完成标准。
- 平台协议、数据库迁移、公开 API、用例 Schema 和适配器 SDK 必须版本化并向后兼容。
- 执行器不得在运行过程中动态安装依赖；运行必须记录 Worker 镜像摘要和执行器版本。
- 状态机、资源锁、租约、取消、超时、清理、变量解析、脱敏和适配器协议必须有根因级自动化测试。
- 不得用无条件重试掩盖失败；首次失败必须保留，重试通过标记为 `flaky`。
- 环境和基础设施失败不得归类为产品失败。

## 首个接入环境

- 用户说“测试环境”时，星火 Agent 目标固定为 `192.168.110.136`。
- 用户说“阿里云”时目标固定为 `120.55.94.4`；它不在第一版回归范围内。
- 不得用一个环境的日志、数据库或运行结果代替另一个环境的证据。
